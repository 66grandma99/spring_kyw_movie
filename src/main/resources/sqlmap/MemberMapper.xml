<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.kosmo.kyw.mapper.MemberMapper">

	<insert id="insertMember">
	<![CDATA[
		insert into member (id, username, name, gender, birth, email, phone, password) values (member_id_seq.nextval, #{username}, #{name}, #{gender}, null, #{email}, #{phone}, #{password})
	]]>
	</insert>

	<resultMap id="userMap" type="edu.kosmo.kyw.vo.MemberVO">
		<result property="id" column="id" />
		<result property="username" column="username" />
		<result property="name" column="name" />
		<result property="gender" column="gender" />
		<result property="birth" column="birth" />
		<result property="email" column="email" />
		<result property="phone" column="phone" />
		<result property="password" column="password" />
		<collection property="authList" resultMap="authMap"></collection>
	</resultMap>

	<resultMap id="authMap" type="edu.kosmo.kyw.vo.AuthVO">
		<result property="member_id" column="member_id" />
		<result property="authority" column="authority" />
	</resultMap>

	<select id="getUser" resultMap="userMap">
		select * from member , authority
		where member.id = authority.member_id and  member.username = #{username}
	</select>

</mapper>